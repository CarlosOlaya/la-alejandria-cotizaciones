# Base de datos PostgreSQL (Neon)
# Recomendado: Usa sslmode=verify-full para mayor seguridad
DATABASE_URL=postgresql://user:password@host/database?sslmode=verify-full

# Ambiente
NODE_ENV=production

# Puerto (Vercel lo asigna automáticamente)
PORT=5000
